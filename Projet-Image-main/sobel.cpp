#include <stdexcept>
/** @file
 * Filtres de Sobel
 **/
#include <cmath>
#include "sobel.hpp"

ImageGris intensiteH(ImageGris img) {
    // Remplacez cette ligne et la suivante par le code adéquat
    throw runtime_error("Fonction intensiteH non implantée ligne 10");
}

ImageGris intensiteV(ImageGris img) {
    // Remplacez cette ligne et la suivante par le code adéquat
    throw runtime_error("Fonction intensiteV non implantée ligne 15");
}

ImageGris intensite(ImageGris img) {
    // Remplacez cette ligne et la suivante par le code adéquat
    throw runtime_error("Fonction intensite non implantée ligne 20");
}

